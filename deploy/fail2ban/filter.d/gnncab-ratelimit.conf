# =============================================================================
# fail2ban filter: gnncab-ratelimit
# Matches HTTP 429 Too Many Requests from Nginx rate limiting zones
# =============================================================================
[INCLUDES]
before = common.conf

[Definition]
# Any request returning 429
failregex = ^<HOST> - \S+ \[.*\] ".*" 429 \d+
ignoreregex =
